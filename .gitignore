# 1. Rác Python (Không bao giờ đẩy lên)
__pycache__/
*.py[cod]
*$py.class

# 2. Môi trường ảo (QUAN TRỌNG: Đẩy cái này lên là hỏng repo)
venv/
env/
.env

# 3. Dữ liệu & Model (GitHub chặn file > 100MB)
# Nếu file .pkl hoặc .pth của bạn < 100MB thì có thể bỏ comment dòng dưới
# data/processed/multiplex_graph.pkl
# checkpoints/*.pth

# 4. Các file kết quả chạy (Tùy chọn: Có thể giữ lại để show kết quả)
# benchmark_results_rem.csv
# rem_performance_chart.png

# 5. IDE Settings
.vscode/
.idea/